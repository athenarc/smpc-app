class AuthenticationService {
  init () {}
  isLoggedIn () {}
  setToken(token) {
    
  }
  getToken() {}
  loggin () {}
  logout () {}

}

// Sigleton Service
const auth = new AuthenticationService()

export default auth
